{
  "scripts": {
    "dev": "air -c .air.toml",
    "start": "go run ./cmd/api",
    "build": "go build -o bin/api ./cmd/api",
    "test": "go test ./...",
    "sqlc:generate": "sqlc generate",
    "migrate": "goose up"
  }
}
